import classNames from 'classnames/bind'
import { FC, memo, useCallback, useMemo, useState } from 'react'
import { Button } from 'antd'

const View:FC = () => {
  return <div>
    <Button>tttttt</Button>
  </div>
}

export default memo(View)
